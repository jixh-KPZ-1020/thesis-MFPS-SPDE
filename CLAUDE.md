# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

This is an academic thesis repository: **thesis-MFPS-SPDE** (likely Multi-Fidelity Physics-Informed Spectral / Stochastic PDE research). The repository is in early setup — source code, dependencies, and build configuration have not yet been committed.

## Getting Started

This section should be updated once the project structure is established. Common items to add:

- How to create/activate the Python virtual environment (e.g., `conda`, `venv`, `uv`)
- How to install dependencies (e.g., `pip install -r requirements.txt` or `pip install -e .`)
- How to run experiments or the main script
- How to run tests (e.g., `pytest` or `python -m pytest tests/`)
- How to lint (e.g., `ruff check .` or `flake8`)

## Remote

```
git@github.com:jixh-KPZ-1020/thesis-MFPS-SPDE.git
```
